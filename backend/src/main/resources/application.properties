# Spring Boot application configuration
server.port=8080
spring.application.name=car-management-backend

# SpringDoc OpenAPI Configuration (for Scalar UI)
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.enabled=false

